name: Kache-only and Nushell-only automation policy

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: nu {0}

jobs:
  automation-policy:
    name: Enforce automation policy
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Install Nushell
        uses: hustcer/setup-nu@ccd5bb5426b05a32009c2ba967946231f3919c97 # v3.25
        with:
          version: 0.113.1
      - name: Reject non-Kache caches and non-Nushell automation
        run: nu ci/gates/automation_policy.nu
