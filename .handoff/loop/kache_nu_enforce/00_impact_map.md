# Impact map

The blast radius is all GitHub Actions jobs and every workflow-invoked script. Cache removal is behavior-preserving except for cold build time; shell enforcement intentionally fails any job not yet ported. Product fixtures may retain foreign-shell text only as inert data and must never execute on this host.
