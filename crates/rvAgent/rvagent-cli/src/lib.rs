//! rvAgent CLI — terminal coding agent with TUI.
//!
//! Provides the `rvagent` binary: an AI coding agent that parses CLI arguments
//! via `clap`, initializes tracing, and dispatches to the appropriate run mode
//! (interactive TUI, single-prompt, or session management).
//!
//! See `main.rs` for the binary entry point.
