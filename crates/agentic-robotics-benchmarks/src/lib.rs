//! agentic-robotics-benchmarks — Benchmarking harness for robotics crates.

#![allow(dead_code, unused_imports, unused_variables)]

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
